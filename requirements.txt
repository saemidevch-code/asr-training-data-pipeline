# Core dependencies
faster-whisper>=0.10.0
pydub>=0.25.1
numpy>=1.24.0

# WhisperX (install separately via git)
# git+https://github.com/m-bain/whisperX.git

# Optional: Acoustic validation
librosa>=0.10.0

# System requirements (not pip-installable):
# - ffmpeg
# - CUDA toolkit (for GPU acceleration)